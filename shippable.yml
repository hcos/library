language: clojure
lein:
  - lein2

install:
  - apt-get install -y diffutils gcc git make patch unzip wget
  - apt-get install -y libev-dev libgeoip-dev libssl-dev
  - apt-get install -y nginx-extras lua-nginx-redis redis-server
  - ./bin/install $HOME

script:
  - ./bin/make-check
